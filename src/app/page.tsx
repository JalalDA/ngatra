import Header from "@/components/navbar";
import { Label, TextInput } from "flowbite-react";

export default function Home() {
  return (
    <div className={`bg-white h-screen w-full flex items-start justify-between`}>
     <Header/>
    </div>
  );
}
